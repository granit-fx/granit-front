/** Current entity-merge wire API version. */
export const API_VERSION = 'v1';

/** Default React Query key prefix for entity-merge queries. */
export const DEFAULT_QUERY_KEY_PREFIX = ['entity-merge'] as const;
