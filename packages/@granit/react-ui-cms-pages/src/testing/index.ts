// Test fixtures for the CMS-pages UI. The page-tree fixture is owned by the
// headless @granit/react-cms package (single source of truth for the contract
// shape); re-exported here so the colocated tests/stories import from one place.
export { mockPageTree, mockSites } from '@granit/react-cms/testing';
