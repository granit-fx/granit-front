/** Default mount path of the Granit.Auditing module API. */
export const DEFAULT_AUDIT_BASE_PATH = '/api/v1/auditing';

/** Default client-side route base for the audit pages (list + detail). */
export const DEFAULT_AUDIT_ROUTE_BASE = '/auditing';
