/**
 * Side-effect module: registering all built-in notification views. Importing
 * this once (via the rendering barrel) populates the registry before any
 * notification is rendered. Add new view modules here.
 */
import './views/test-notification';
import './views/activity-notifications';
