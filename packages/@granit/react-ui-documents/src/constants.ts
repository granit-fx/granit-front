/**
 * Re-export of `@granit/documents` permissions under a stable admin-UI alias so
 * consumers don't need to import the headless framework package directly when
 * wiring routes and nav entries.
 */
export { DocumentsPermissions as DOCUMENTS_PERMISSIONS } from '@granit/documents';
