<img src="https://granit-fx.dev/images/granit-icon.svg" alt="" height="32" align="left" style="margin-right:10px" />

# @granit/data-exchange

Tabular data exchange (headless): export (`useExportJob`, `useExportPresets`, `useExportFields`) and import (`useImportJob`, `useImportPreview`, `useImportReport`) with types mirroring the `Granit.DataExchange` .NET contract.

Part of the [Granit](https://granit-fx.dev) framework.

## Installation

Consumed via `link:` protocol — see the [integration documentation](https://granit-fx.dev).

## Documentation

See the [full documentation](https://granit-fx.dev/frontend/business/data-exchange/).
